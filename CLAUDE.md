# time-keeper-windows — AI 작업 규칙

이 파일은 Claude Code / Cursor 등 AI 코딩 도구가 이 레포에서 작업할 때 지켜야 할 규칙이다.

## 프로젝트 성격

가족이 공유하는 윈도우 PC에서 각자의 화면 사용 시간을 다루는 도구.
**감시·차단 도구가 아니다. 자기 조절 연습 도구다.**

이 구분이 코드 결정의 대부분을 좌우한다. 구현 방법을 고를 때 "더 확실하게 막는 방법"이 아니라 "더 잘 보이게 하는 방법"을 택한다.

## 절대 구현하지 않는다

사용자가 요청하더라도, 아래 항목은 구현 전에 반드시 되묻는다. 이 프로젝트의 설계 전제를 뒤집는 변경이기 때문이다.

- 프로세스 숨기기, 프로세스 이름 위장, 트레이 아이콘 제거
- 작업관리자 종료 방어, 종료 감지 후 재실행하는 워치독
- 전역 키보드·마우스 입력 차단 후킹 (`SetWindowsHookEx` 등)
- 관리자 권한 상승 요구 (UAC manifest `requireAdministrator`)
- **시간이 끝났다고 앱이 임의로** 강제 종료·강제 로그오프·강제 절전하는 일.
  예외: 아이가 '오늘은 여기까지'를 직접 고른 뒤의 마무리 절전/종료는 자기 조절의
  일부로 허용한다(2026-07-30 사용자 결정, SPEC 7절). 이 경계를 넘지 않는다 —
  time-up 시 자동 종료·절전은 여전히 금지다.
- 웹사이트·앱 차단, 방문 기록 수집, 화면 캡처, 키 입력 기록
- 원격 제어 채널 (텔레그램 봇, 웹훅, 소켓 서버 등)
- 시스템 시계 변경 감지 및 방어

이유는 [SPEC.md](SPEC.md) 12절과 [README.md](README.md) 참고.

## 반드시 지킨다

- **잔여시간 위젯에 닫기·숨기기 기능을 만들지 않는다.** 항상 보이는 것이 핵심 기능이다.
- 상태 파일 쓰기는 원자적으로 한다. 임시 파일 → `rename`.
- 부모 PIN과 개인 PIN은 salt 포함 해시로만 저장한다. 평문·단순 MD5 금지.
- 개인 PIN은 '누가 쓰는지' 확인용이다. 잠금으로 강화하지 않는다.
- 사용자 데이터 파일(`state.json`, `config.json`, `log.csv`)은 절대 커밋되지 않게 한다.
- 사용자에게 보이는 모든 문구는 한국어. 명령형·질책형 표현을 쓰지 않는다.
  - 나쁨: `시간 초과! 사용이 차단되었습니다.`
  - 좋음: `오늘 사용 시간이 끝났어요.`

## 범위 관리

SPEC.md에 없는 기능을 임의로 추가하지 않는다. 필요하다고 판단되면 코드를 쓰기 전에 SPEC.md 수정을 제안한다.

다중 사용자(가족 5인)·개인 PIN·앱 내 기록 보기는 2026-07-29 사용자 결정으로
범위에 들어왔다(SPEC.md 2·10절). 다음은 여전히 MVP 밖이다. 요청 없이 넣지 않는다.

- 유휴 감지 (무입력 시 카운트 정지)
- 다국어
- 차트 라이브러리 (기록 보기는 tkinter Canvas로 직접 그린다)
- 분 단위 상세 기록, SQLite 전환

## 코드 스타일

- 의존성을 최소로 유지한다. 외부 패키지는 **customtkinter 하나뿐**이고, 이것은
  "투박하지 않은 디자인" 요구로 사용자가 승인한 예외다. 그 밖에는 표준 라이브러리로
  되는 일에 패키지를 추가하지 않는다.
- 파일 수를 늘리기보다 한 파일을 읽기 쉽게 유지한다. 전체가 `time_keeper.pyw` 한 파일이다.
  다중 사용자·대시보드·마무리 절전이 더해지며 ~1,400줄이 됐다(처음 300줄 목표는 MVP 범위가
  넓어지며 갱신됨). 새 기능으로 파일이 커지면 파일을 쪼개기보다 섹션 구분과 주석으로 읽기 쉽게 둔다.
- 주석은 "무엇을" 대신 "왜"를 적는다. 특히 위 금지 항목과 관련된 결정에는 이유를 남긴다.

## 기술 스택

**확정 (2026-07-29):** Python 3.10+ / tkinter + customtkinter.
exe를 만들지 않는다 — `.pyw` + 시작프로그램 바로가기로 배포해 백신 오탐을 회피한다.
저장은 JSON + CSV. DB(SQLite 포함)를 쓰지 않는다.
