# time-keeper-windows — AI 작업 규칙

이 파일은 Claude Code / Cursor 등 AI 코딩 도구가 이 레포에서 작업할 때 지켜야 할 규칙이다.

## 프로젝트 성격

가족이 공유하는 윈도우 PC에서 자녀의 화면 사용 시간을 다루는 도구.
**감시·차단 도구가 아니다. 자기 조절 연습 도구다.**

이 구분이 코드 결정의 대부분을 좌우한다. 구현 방법을 고를 때 "더 확실하게 막는 방법"이 아니라 "더 잘 보이게 하는 방법"을 택한다.

## 절대 구현하지 않는다

사용자가 요청하더라도, 아래 항목은 구현 전에 반드시 되묻는다. 이 프로젝트의 설계 전제를 뒤집는 변경이기 때문이다.

- 프로세스 숨기기, 프로세스 이름 위장, 트레이 아이콘 제거
- 작업관리자 종료 방어, 종료 감지 후 재실행하는 워치독
- 전역 키보드·마우스 입력 차단 후킹 (`SetWindowsHookEx` 등)
- 관리자 권한 상승 요구 (UAC manifest `requireAdministrator`)
- 강제 종료, 강제 로그오프, 강제 절전
- 웹사이트·앱 차단, 방문 기록 수집, 화면 캡처, 키 입력 기록
- 원격 제어 채널 (텔레그램 봇, 웹훅, 소켓 서버 등)
- 시스템 시계 변경 감지 및 방어

이유는 [SPEC.md](SPEC.md) 9절과 [README.md](README.md) 참고.

## 반드시 지킨다

- **잔여시간 위젯에 닫기·숨기기 기능을 만들지 않는다.** 항상 보이는 것이 핵심 기능이다.
- 상태 파일 쓰기는 원자적으로 한다. 임시 파일 → `rename`.
- `parent_pin_hash`는 salt 포함 해시로만 저장한다. 평문·단순 MD5 금지.
- 사용자 데이터 파일(`state.json`, `config.json`, `log.csv`)은 절대 커밋되지 않게 한다.
- 사용자에게 보이는 모든 문구는 한국어. 명령형·질책형 표현을 쓰지 않는다.
  - 나쁨: `시간 초과! 사용이 차단되었습니다.`
  - 좋음: `오늘 사용 시간이 끝났어요.`

## 범위 관리

SPEC.md에 없는 기능을 임의로 추가하지 않는다. 필요하다고 판단되면 코드를 쓰기 전에 SPEC.md 수정을 제안한다.

특히 다음은 MVP에서 명시적으로 제외되어 있다. 요청 없이 넣지 않는다.

- 유휴 감지 (마우스 무입력 시 카운트 정지)
- 앱 내 통계 대시보드, 차트
- 여러 자녀 개별 관리
- 다국어

## 코드 스타일

- 의존성을 최소로 유지한다. 표준 라이브러리로 되는 일에 패키지를 추가하지 않는다.
- 파일 수를 늘리기보다 한 파일을 읽기 쉽게 유지한다. MVP 전체가 300줄 안에 들어가는 것이 목표다.
- 주석은 "무엇을" 대신 "왜"를 적는다. 특히 위 금지 항목과 관련된 결정에는 이유를 남긴다.

## 기술 스택

**미확정.** 스택을 전제한 코드를 작성하기 전에 사용자에게 확인한다.
후보: C# WPF (.NET 8) / Python + tkinter / Tauri (Rust + React)
