# time-keeper-windows — MVP 스펙

이 문서가 구현의 기준이다. 코드와 어긋나면 이 문서를 먼저 고친다.

> 2026-07-29 범위 확정: 다중 사용자(가족 5인), 개인 PIN, 앱 내 기록 보기,
> '5분만 더'의 사람별 규칙이 사용자 결정으로 MVP에 들어왔다.
> 스택 확정: Python + customtkinter. 저장은 JSON + CSV 유지.

---

## 1. 기술 스택 (확정)

- Python 3.10+ / tkinter + **customtkinter** — customtkinter는 유일한 외부 패키지.
  "투박하지 않은 디자인" 요구로 표준 라이브러리 원칙에 사용자가 승인한 예외다.
- 실행은 `time_keeper.pyw` 더블클릭(pythonw, 콘솔 창 없음). **exe를 만들지 않는다.**
  서명 없는 exe의 백신 오탐 문제를 통째로 회피하기 위한 선택이다.
- 저장은 JSON(설정·오늘 상태) + CSV(일별 기록). DB를 쓰지 않는다. SQLite도 쓰지
  않는다 — 파일을 메모장·엑셀로 열면 그냥 읽혀야 한다는 원칙 때문이다.

---

## 2. 사용자 (프로필)

가족 여러 명(기본 5명)이 **한 윈도우 계정**을 같이 쓴다는 전제다.

- 프로필마다: 이름 / 개인 PIN(선택) / 평일·주말 한도 / 한도 없음(기록만) 여부 /
  '5분만 더' 하루 횟수 / 연속 사용 허용 여부.
- 개인 PIN은 '누가 쓰는지'의 본인 확인용이다. **잠금이 아니다.** 다른 가족 이름으로
  쓰는 실수·장난을 막아 기록을 공정하게 만드는 것이 목적. 어른 프로필은 PIN 없이 써도 된다.
- 앱이 시작되면(부팅이든 재실행이든) **항상** "누가 컴퓨터를 쓸까요?" 선택 창이 뜬다.
  이전 사용자를 기억해 이어가지 않는다 — 다른 가족이 켰을 때 남의 이름으로 기록되는 것을
  막기 위해서다. 그날의 사용량 카운터 자체는 유지된다.
- **아무도 선택하지 않으면 시간은 누구에게도 기록되지 않는다.** 위젯이
  "사용자를 선택해 주세요" 상태로 계속 보일 뿐, 강제하지 않는다. 이 앱은 잠금 화면이 아니다.
- 사용자 전환은 위젯 메뉴·오버레이의 "사용자 바꾸기"로. 전환하면 이전 사용자의 일시정지는 해제된다.
- 프로필 추가·이름·한도 변경·삭제는 설정 화면에서, 부모 PIN 입력 후에 한다.
  이름은 서로 겹칠 수 없다(기록이 이름 기준이라). 최소 한 명은 남긴다.
  삭제하면 그 시점까지의 오늘 기록을 CSV에 남기고 오늘 상태를 지운다.

---

## 3. 상태 저장

파일 세 개로 관리한다. DB를 쓰지 않는다.

```
%LOCALAPPDATA%\time-keeper-windows\config.json   설정·프로필
%LOCALAPPDATA%\time-keeper-windows\state.json    오늘 상태
%LOCALAPPDATA%\time-keeper-windows\log.csv       일별 기록
```

```json
// config.json
{
  "parent_pin_hash": null,
  "warn_at_minutes": [10, 5, 1],
  "extension_minutes": 5,
  "extension_cooldown_minutes": 30,
  "pause_auto_resume_minutes": 30,
  "profiles": [
    {
      "id": "p1",
      "name": "첫째",
      "pin_hash": null,
      "limit_weekday": 60,
      "limit_weekend": 120,
      "no_limit": false,
      "max_extensions_per_day": 3,
      "allow_consecutive_extensions": true
    }
  ]
}
```

```json
// state.json
{
  "date": "2026-07-29",
  "current_user": "p1",
  "users": {
    "p1": {
      "used_minutes": 42,
      "extra_minutes": 5,
      "extensions_self": 1,
      "extensions_parent": 0,
      "pause_minutes": 10,
      "paused": false,
      "pause_started": null,
      "last_self_extension": "2026-07-29T16:40:00",
      "extension_ended": null
    }
  }
}
```

- `extension_ended`는 주어진 시간이 실제로 다 떨어진 시각. '연속 금지'의 재사용
  대기(7절)가 이 시각을 기준으로 계산된다. 연장을 받으면 다시 비워진다.

- `extra_minutes`(연장으로 늘어난 분)를 상태에 저장하는 이유: 재부팅해도 연장이 유지되어야 한다.
- `date`가 오늘과 다르면 → 전날 기록을 log.csv에 남기고 모든 카운터를 리셋, `current_user`도 비운다.
- 쓰기는 **원자적으로** 한다. 임시 파일에 쓰고 `rename`. 강제 종료와 겹쳐도 파일이 깨지지 않게.
- 파일이 없거나 파싱 실패 → 경고 없이 새로 만든다. 사용 시간은 잃어도 되는 데이터다.
- `parent_pin_hash`·`pin_hash`는 평문 저장 금지. PBKDF2 등 salt 포함 해시.
  어차피 우회 가능한 앱이지만, PIN을 재사용하는 습관 때문에라도 평문은 남기지 않는다.

---

## 4. 시간 카운트

- **정의: 앱이 실행 중이고, 그 사람이 선택되어 있고, 일시정지가 아닌 시간.** 이 이상 복잡하게 만들지 않는다.
- 60초 주기 타이머로 현재 사용자의 `used_minutes += 1`. 매 틱마다 state.json 저장.
- 유휴 감지(무입력 시 정지)는 **v2로 미룬다.**
- 앱 시작 시 state.json을 읽어 이어서 센다. 재부팅으로 초기화되지 않는다.
- 시계를 되돌리는 우회는 방어하지 않는다. (신뢰 기반 원칙)
- 중복 실행은 뮤텍스로 하나만 남긴다. 숨기려는 게 아니라 **이중 카운트 방지** 목적이다.

### 요일별 한도

- 월~금 → `limit_weekday`, 토·일 → `limit_weekend` (프로필별)
- 자정 기준, 로컬 시간으로 판정.
- `no_limit` 프로필은 한도·경고·오버레이 없이 기록만 한다.

---

## 5. 잔여시간 위젯

- 화면 우측 상단, 항상 위(topmost), 작고 둥근 카드. `이름 · 43분 남음`
- 색상 단계: 10분 초과 → 기본(어두운 남색) / 10분 이하 → 주황 / 3분 이하 → 빨강.
  일시정지 중에는 `이름 · 일시정지 중 · N분 후 다시 시작`.
- **닫기·숨기기 기능을 두지 않는다.** 위젯이 보이는 것 자체가 이 앱의 핵심 기능이다.
- 드래그로 옮길 수 있다. 게임 UI를 가리면 짜증만 남는다.
- 우클릭 메뉴: 일시정지/다시 시작, 사용자 바꾸기, 기록 보기, 설정(부모).
  더블클릭 → 기록 보기.

---

## 6. 사전 경고

`warn_at_minutes`의 각 시점에 한 번씩만 알린다. (발동 여부는 메모리에 기록,
연장이 생기면 다시 알린다)

- 화면 위쪽 가운데에 알림 배너 + 위젯 색 변화. 배너는 8초 뒤 스스로 사라진다.
  윈도우 토스트 대신 배너를 쓰는 이유: 외부 패키지 없이 같은 효과를 내기 위해서다.
- 문구는 명령형이 아니라 정보 전달형으로:
  - `10분 남았어요. 저장할 게 있으면 지금 해두세요.`
  - `1분 남았어요.`

---

## 7. 시간 종료 오버레이

- 모든 모니터를 덮는 전체화면 창. topmost.
- **키보드·마우스 입력 후킹은 하지 않는다.** 창을 띄우는 것까지만.
  Alt+Tab이나 작업 관리자로 벗어날 수 있어도 괜찮다.
- 화면 구성
  - `오늘 사용 시간이 끝났어요` + 이름·오늘 총 사용 시간
  - `5분만 더` 버튼 — 비밀번호 없이. 아래 규칙을 따른다.
  - `부모 PIN으로 연장` 버튼 — PIN 입력 후 +15 / +30 / +60분
  - `사용자 바꾸기` 버튼 — 다른 가족이 이어서 쓸 수 있게
  - `오늘은 여기까지` 버튼 — 화면은 그대로 덮여 있다 (컴퓨터를 강제 종료하지 않는다)

### '5분만 더' 규칙 (사람별)

- 하루 `max_extensions_per_day`회 (기본 3회). 누르면 기록에 남는다.
- `allow_consecutive_extensions`가 **켜져 있으면**: 연속으로 눌러 5+5+5분처럼 이어 쓸 수 있다.
- **꺼져 있으면**: 한 번 쓴 5분이 **실제로 끝난 시각**(`extension_ended`) 기준으로
  `extension_cooldown_minutes`(기본 30분)가 지나야 다음 5분을 쓸 수 있다.
  중간에 일시정지해서 늦게 끝나면 대기도 그만큼 뒤로 밀린다. "한도는 끝났지만 나중에 중요한 검색이 생겼을 때
  잠깐 다시 쓰는" 용도가 된다. 대기 중에는 버튼을 비활성화하고 몇 분 뒤에 쓸 수 있는지 보여준다.
- 횟수를 다 쓰면 버튼을 비활성화하고, 남은 방법은 부모 PIN뿐임을 안내한다.

### '5분만 더'를 PIN 없이 허용하는 이유

이 앱은 강제가 아니라 자기 조절 연습 도구다. 스스로 결정하게 하고, 그 결정이
기록으로 남아 주말 대화의 재료가 되게 한다. 남용이 반복되면 횟수를 줄이는
대화를 하면 된다. 코드로 막을 문제가 아니다.

---

## 8. 일시정지

- 위젯 메뉴에서 본인이 직접 누른다. PIN 불필요.
- 일시정지 중에는 위젯이 `일시정지 중`으로 바뀐다. 숨기지 않는다.
- `pause_auto_resume_minutes`(기본 30분) 경과 후 자동 재개. (켜두고 잊어버리는 걸 막는 용도)
- 일시정지 누적 시간을 기록한다. 하루 총량 제한은 두지 않는다. 기록에 남는 것으로 충분하다.

---

## 9. 사용 기록 (CSV)

한 줄씩 append. 헤더 포함. 새 파일에만 BOM(utf-8-sig)을 넣어 엑셀이 한글을 바로 읽게 한다.

```csv
date,user,used_minutes,limit_minutes,extensions_self,extensions_parent,pause_minutes
2026-07-29,첫째,65,60,1,0,15
```

- 날짜가 바뀔 때(자정 또는 다음 실행 시) 전날 사용자별로 한 줄씩 기록한다.
  `limit_minutes`는 그날의 기본 한도(연장 제외), 한도 없는 프로필은 빈칸.
- 파일은 사람이 그냥 열어봐도 읽히는 형태를 유지한다. 자녀가 열어보는 것도 괜찮다.
- 이름을 바꾸면 과거 행은 옛 이름 그대로 남는다. 기록은 사람이 읽는 문서이지 DB가 아니다.
- 파일이 다른 프로그램(엑셀 등)에 잠겨 있어 쓰기가 실패하면 그날 기록은 조용히 건너뛴다.
  기록 한 줄 때문에 타이머를 멈추지 않는다.

---

## 10. 기록 보기 (대시보드)

- 위젯 메뉴 "기록 보기" 또는 위젯 더블클릭으로 연다. 이 창은 보통 창이라 닫아도 된다.
- 구성
  - **오늘**: 가족 전원의 사용량 막대 (한도 대비, 초과는 빨강, 한도 없음은 분만 표시)
  - **이번 주**: 선택한 가족의 요일별 막대그래프
  - **최근 4주**: 선택한 가족의 주별 합계 막대그래프
- 차트는 tkinter Canvas로 직접 그린다. **차트 라이브러리를 추가하지 않는다.**
- 데이터 원본은 log.csv + 오늘 state. 더 깊은 분석은 CSV를 엑셀로 열면 된다.

---

## 11. 자동 실행

- `shell:startup` 폴더에 바로가기를 두는 방식. 사용자가 눈으로 확인하고 지울 수 있는
  위치를 레지스트리보다 우선한다.
- 자동 등록을 앱이 몰래 하지 않는다. 설치 문서에 수동 절차를 적고,
  설정 화면에 `자동 실행: 등록하기/해제하기` 버튼을 둔다.

---

## 12. 하지 않을 것 (재확인)

구현 중 "이거 있으면 더 확실한데"라는 생각이 들 때 이 목록을 본다.

- 프로세스 숨기기, 이름 위장
- 작업관리자 종료 방어, 감시용 워치독 프로세스
- 전역 키보드·마우스 입력 차단 후킹
- 관리자 권한 상승 요구
- 강제 종료 / 강제 로그오프
- 사이트·앱 차단, 방문 기록 수집
- 원격 제어 (텔레그램 봇 등)
- 시계 변경 감지 및 방어
- 개인 PIN을 '뚫리지 않는 잠금'으로 강화하는 일 — PIN은 신원 확인용이다

---

## 13. 남겨둔 것 (v2 이후)

- 유휴 감지 (무입력 시 카운트 정지)
- 다국어 (한국어만으로 시작)
- 분 단위 상세 기록·복잡한 통계 (필요해지면 그때 SQLite 검토)
