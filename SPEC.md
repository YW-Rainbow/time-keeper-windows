# 시간지기 — MVP 스펙

이 문서가 구현의 기준이다. 코드와 어긋나면 이 문서를 먼저 고친다.

---

## 1. 상태 저장

파일 하나로 관리한다. DB를 쓰지 않는다.

```
%LOCALAPPDATA%\sigan-jigi\state.json
```

```json
{
  "date": "2026-07-29",
  "used_minutes": 42,
  "paused": false,
  "extensions_today": 1,
  "pause_minutes_today": 15
}
```

- `date`가 오늘과 다르면 → 모든 카운터를 0으로 리셋하고 `date`를 갱신한다.
- 쓰기는 **원자적으로** 한다. 임시 파일에 쓰고 `rename`. 강제 종료 중 쓰기가 겹쳐도 파일이 깨지지 않게.
- 파일이 없거나 파싱 실패 → 경고 없이 새로 만든다. 사용 시간 데이터는 잃어도 되는 데이터다.

설정은 별도 파일로 분리한다.

```
%LOCALAPPDATA%\sigan-jigi\config.json
```

```json
{
  "limit_weekday_minutes": 60,
  "limit_weekend_minutes": 120,
  "warn_at_minutes": [10, 5, 1],
  "extension_minutes": 5,
  "max_extensions_per_day": 3,
  "parent_pin_hash": null,
  "pause_auto_resume_minutes": 30
}
```

`parent_pin_hash`는 평문 저장 금지. PBKDF2 또는 bcrypt 등 salt 포함 해시. 어차피 우회 가능한 앱이지만, PIN을 재사용하는 습관 때문에라도 평문은 남기지 않는다.

---

## 2. 시간 카운트

- **정의: 앱이 실행 중이고 일시정지 상태가 아닌 시간.** 이 이상 복잡하게 만들지 않는다.
- 60초 주기 타이머로 `used_minutes += 1`. 매 틱마다 state.json 저장.
- 유휴 감지(마우스·키보드 무입력 시 카운트 정지)는 **v2로 미룬다.** MVP에 넣지 않는다.
- 앱 시작 시 state.json을 읽어 이어서 센다. 재부팅으로 초기화되지 않는다.
- 시계를 뒤로 돌려 우회하는 경우는 방어하지 않는다. (신뢰 기반 원칙)

### 요일별 한도

- 월~금 → `limit_weekday_minutes`
- 토, 일 → `limit_weekend_minutes`
- 자정 기준으로 판정. 요일 판정은 로컬 시간.

---

## 3. 잔여시간 위젯

- 화면 우측 상단, 항상 위(topmost), 작고 반투명. 클릭 통과(click-through)면 더 좋다.
- 표시 형식: `43분 남음`
- 색상 단계
  - 10분 초과 → 기본색 (차분한 회색/파랑)
  - 10분 이하 → 주황
  - 3분 이하 → 빨강
- **닫기 버튼을 두지 않는다.** 위젯이 보이는 것 자체가 이 앱의 핵심 기능이다.
- 위치는 드래그로 옮길 수 있게. 게임 UI를 가리면 짜증만 남는다.

---

## 4. 사전 경고

`warn_at_minutes`의 각 시점에 한 번씩만 알린다. (같은 시점 중복 알림 금지 → 발동 여부를 메모리에 기록)

- 윈도우 토스트 알림 + 위젯 색 변화
- 문구는 명령형이 아니라 정보 전달형으로:
  - `10분 남았어요. 저장할 게 있으면 지금 해두세요.`
  - `1분 남았어요.`

---

## 5. 시간 종료 오버레이

- 모든 모니터를 덮는 전체화면 창. topmost.
- **키보드·마우스 입력 후킹은 하지 않는다.** 창을 띄우는 것까지만. Alt+Tab으로 빠져나갈 수 있어도 괜찮다.
- 화면 구성
  - `오늘 사용 시간이 끝났어요` + 오늘 총 사용 시간
  - `5분만 더` 버튼 — PIN 없이 누를 수 있다. 단, 하루 `max_extensions_per_day`회까지. 누르면 기록에 남는다.
  - `부모 PIN으로 연장` 버튼 — PIN 입력 시 +15 / +30 / +60분
  - `종료` 버튼 — 오버레이 유지 (컴퓨터를 강제 종료시키지 않는다)
- 연장 횟수를 다 쓰면 `5만분 더` 버튼을 비활성화하고, 남은 방법은 PIN뿐임을 안내한다.

### '5분만 더'를 PIN 없이 허용하는 이유

이 앱은 강제가 아니라 자기 조절 연습 도구다. 하루 3번까지는 스스로 결정하게 하고, 그 결정이 기록으로 남아 주말 대화의 재료가 되게 한다. 남용이 반복되면 `max_extensions_per_day`를 줄이는 대화를 하면 된다. 코드로 막을 문제가 아니다.

---

## 6. 일시정지

- 위젯 또는 트레이 메뉴에서 자녀가 직접 누를 수 있다. PIN 불필요.
- 일시정지 중에는 위젯이 `일시정지 중` 으로 바뀐다. 숨기지 않는다.
- `pause_auto_resume_minutes` 경과 후 자동 재개. (켜두고 잊어버리는 걸 막는 용도)
- 일시정지 누적 시간을 기록한다. 하루 총량 제한은 두지 않는다. 기록에 남는 것으로 충분하다.

---

## 7. 사용 기록 (CSV)

```
%LOCALAPPDATA%\sigan-jigi\log.csv
```

한 줄씩 append. 헤더 포함.

```csv
date,used_minutes,limit_minutes,extensions_self,extensions_parent,pause_minutes
2026-07-29,65,60,1,0,15
```

- 하루가 끝날 때(날짜 변경 감지 시) 전날 한 줄을 기록한다.
- 파일은 사람이 그냥 열어봐도 읽히는 형태를 유지한다. 자녀가 열어보는 것도 괜찮다.
- 앱 안에 대시보드를 만들지 않는다. **엑셀로 열면 된다.** MVP에 차트 넣지 않기.

---

## 8. 자동 실행

- `shell:startup` 폴더에 바로가기를 두는 방식. 레지스트리 `HKCU\Run` 도 무관하나, 사용자가 눈으로 확인하고 지울 수 있는 startup 폴더를 우선한다.
- 자동 등록을 앱이 몰래 하지 않는다. 설치 안내 문서에 수동 절차를 적고, 편의 기능으로 `설정 → 자동 실행 등록` 버튼을 둔다.

---

## 9. 하지 않을 것 (재확인)

구현 중 "이거 있으면 더 확실한데" 라는 생각이 들 때 이 목록을 본다.

- 프로세스 숨기기, 이름 위장
- 작업관리자 종료 방어, 감시용 워치독 프로세스
- 전역 키보드·마우스 입력 차단 후킹
- 관리자 권한 상승 요구
- 강제 종료 / 강제 로그오프
- 사이트·앱 차단, 방문 기록 수집
- 원격 제어 (텔레그램 봇 등)
- 시계 변경 감지 및 방어

---

## 10. 미확정

- **기술 스택** — C# WPF(.NET 8) / Python+tkinter / Tauri 중 미정
- 다국어 (한국어만으로 시작)
- 유휴 감지 (v2)
- 여러 자녀를 한 PC에서 개별 관리 (v2 이후, 필요해지면)
